name: CI

on: [push]

jobs:
  sast_scan:
    name: Run Bandit Scan
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4  # Updated to latest version

    - name: Set up Python  # Fixed typo
      uses: actions/setup-python@v4  # Updated to latest version
      with:
        python-version: 3.8

    - name: Install Bandit
      run: pip install bandit

    - name: Run Bandit Scan
      run: bandit -ll -ii -r . -f json -o bandit-report.json

    - name: Upload Artifact
      uses: actions/upload-artifact@v4  # Ensuring latest stable version
      if: always()
      with:
        name: bandit-findings
        path: bandit-report.json

 
  image_scan:
     name: Build and run on image scan
     runs-on: ubuntu-latest

     steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Updated to latest version

      - name: Build Docker Image
        run: docker build -t myapp:latest .  # Fixed missing build context

      - name: Docker Scout Scan
        run: 
          curl -fsSL https://raw.githubusercontent.com/docker/scout-cli/main/install.sh -o install-scout.sh
          sh install-scout.sh
          docker scout quickview
          docker scout cves


        
